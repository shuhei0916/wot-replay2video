# todo.md — タスク管理

このファイルでプロジェクトのタスクを管理する（将来タスクも含む）。
完了したら「完了済み」セクションに移す。

---

## 次にやる

- [ ] **録画価値フィルタをWN8等の階級/車両非依存な指標に置き換える**（2026-08-01着想）:
      現行の`meets_criteria`（`min_kills`/`min_damage`/`min_mastery`の絶対値OR条件）は
      低ティア車両を構造的に不利にする。低ティアは元々のHP・ダメージ量の絶対値が
      小さいため、同等以上の活躍でも高ティアより閾値を満たしにくい。
      WN8等の「車両ごとの期待値（expected values）」に対する相対評価にすれば、
      ティア・車種に依存せず「その車両として上出来だったか」で判定できる。
      **実現可能性の下調べ**: リプレイBlock1に`xvm`フィールドが存在するが、実際に
      サンプルした複数のリプレイでは`null`（XVM mod未使用のため）で、期待値データは
      リプレイにもクライアントにも同梱されていないと確認済み。よって
      `tools/extract_vehicle_names.py`等（クライアント同梱データから抽出）と同じ
      手法は使えず、外部の期待値データセット（例: wot-news.com系のWN8計算用
      JSON、コミュニティ管理・不定期更新）を別途取得して同梱する必要がある。
      クライアントバージョン変更時の車両名/勲章データ更新（`tools/extract_*`）とは
      別に、この期待値データ自体の更新運用も発生する点に留意
- [ ] **YouTube Studio で無音の3本を削除**（アップロードスコープの OAuth では API 削除
      不可のため手動で）: BHPe3ycmFAQ / z-jQb2ZVg0Q / 0yTtrVBpNec
      （upload_backlog.py の初回実行時、無音ガード追加前に旧 Shorts が混入した）
- [ ] 毎日 `python -u upload_backlog.py` を実行してバックログを消化
      （2026-07-13 時点で **57本待ち**、最終アップロードは 2026-07-07 00:52。
      アップロードは「新規リプレイ処理時のインライン」か「このスクリプトの手動実行」
      でのみ発生し、**自動では一切走らない**ため、実行しない日は 0 本になる。
      チャンネルの日次アップロード上限は実測 ~19本/日（uploadLimitExceeded）。
      **「APIクォータは実質6本/日」という記述は2026-08-04時点で誤りと判明**:
      `output/uploaded.json`のタイムスタンプ集計で直近5日間 9〜19本/日を
      クォータエラーなしで安定アップロードできている実績を確認。ボトルネックは
      実質的にチャンネルの日次アップロード上限（~19本）の方。max_per_run は現在 5。
      タスクスケジューラ化の本命候補）
- [x] 無音期間の再録画バッチ完走（2026-07-05 04:10 全処理完了、processed.json 72本。
      品質フィルタ通過分すべて処理、一過性スキップも回収済み）
- [x] `youtube.privacy` を public 化し公開運用開始（2026-07-04 品質承認、初日19本公開）
- [x] **ランダム戦以外（オンスロート・ストーリーモード・ラストスタンド等）を動画化対象から
      除外**（2026-08-01実装）: Block 1 の `battleType`（ランダム戦=1、実測でストーリー
      モード/エピックランダムは104と確認）で判定。当初「非ランダム戦だけ成績閾値を厳しく
      する」案を検討したが、それでは「非ランダム戦の“通常の”見せ場試合」を除外できない
      とのユーザー指摘があり、**成績に関わらず一律除外**に変更。`config.yaml`の
      `batch.random_battles_only`（既定true）でいつでも旧挙動に戻せる。
      `src/parse_replay.py`（`BattleInfo.battle_type`/`is_random_battle`）、
      `src/batch.py`（`_select_worthy`）
- [ ] **HPバーオーバーレイのサイズ・位置を微調整**（2026-08-04着想）: 現状の
      `shorts.hp_overlay`（src矩形、dst幅・y座標）は2026-07-19に一度見た目承認したもの。
      改めて微調整の余地ありとの声
- [ ] **初弾後・大移動時にマップを表示**（2026-08-04着想）: (a) 初弾（最初の射撃）の後に
      ミニマップ相当を映すと、視聴者が「どのマップのどの位置での戦闘か」を把握しやすくなる
      のではという仮説。(b) 大きく移動する区間は、間延び防止のため倍速再生等の活用を検討
- [ ] **OAuth再認証のヘルパースクリプト**（2026-08-04着想）: Testing公開ステータスの
      ためrefresh_tokenが7〜15日程度で失効し、その都度`config/token.json`のリネーム→
      `flow.run_local_server()`によるブラウザ再認証、という手動手順が発生している。
      退避＋再認証トリガーをワンコマンド化する小さいスクリプトを作る
- [ ] **人気Shortsのタイトルを収集しておく（将来のAIタイトル生成の参考用）**
      （2026-08-04着想）: 2026-07-17に「LLM不使用」の方針を確定した際、
      「人気タイトルのAPI収集案（few-shot用）もLLM不使用につき見送り」としていたが、
      これを再開する。今すぐLLMタイトル生成に戻すかは未定だが、再生数の多いWoT系
      Shortsのタイトルを先に収集・蓄積しておけば、将来AIベースのタイトル生成に
      切り替える際にfew-shot参考データとしてすぐ使える

## 改善

- [ ] **クリップ構成に黄金比を試す**（2026-07-25 着想）: pre/post の秒数配分
      （現行 CLIP_PRE_SEC=3.0 / CLIP_POST_SEC=4.0）や、動画全体の尺配分
      （フック部分と本編の比率等）に黄金比（1:1.618）を使うと、より心地よく
      「つい見てしまう」構成になるのではという仮説。pre/post に限らず適用箇所は
      未定。具体案を詰めてから小さく試して比較する
- [x] **HP バーのオーバーレイ表示**（2026-07-19 実装）: 左下 HUD の HP バー
      （1920x1080 実測 x=3, y=873, 224×18）を毎フレーム切り出し、縦動画上部
      （幅 874px 中央寄せ、上端 y=300 ≒ 画面上 15.6〜19.5%）に lanczos 拡大で重畳。
      config は `shorts.hp_overlay`（enabled / src / dst）。適用は新規編集分から。
      **HUD スケールや録画解像度を変えたら src 矩形の再計測が必要**
- [x] **タイトルの車両名を公式名に修正**（2026-07-22 実装）:
      `tools/extract_vehicle_names.py` が scripts.pkg の item_defs/vehicles と
      各 nation の `_vehicles.mo` から `src/data/vehicle_names.json`（959件）を生成、
      `src/vehicle_names.py` がランタイムで解決（未収録タグは従来の文字列加工に
      フォールバック）。マップ名（`mapDisplayName`）は元々リプレイ内蔵の公式名で
      対応不要だった。クライアント更新後、新規車両を反映するにはツール再実行が必要
      （achievements と同じ運用）
- [x] **0キル時はタイトルからキル数を省く**（2026-07-20 実装）:
      `generate_title()` で kills==0 のとき項目ごと省略。テスト追加
- [x] **プレイヤー死亡時の録画早期打ち切り**（2026-07-20 実装・実機確認済み）:
      mod v3 の `Vehicle.onHealthChanged` フックで自車撃破を death イベントとして
      記録し、`recording.death_cutoff.grace_sec`（既定20秒）経過で録画を打ち切る。
      2026-07-20/21 のバッチで計17本発火、録画時間を数分単位で短縮
- [x] **開幕カウントダウンの定型スキップ**（2026-07-20 実装・実機確認済み）:
      `ReplayBattleSpace` 検出後に右矢印キー×2（+20秒シーク）を送信
      （`recording.intro_skip`）。動的スキップ（イベント位置ベース）は
      「ハイライト位置が再生前に分からない」「スキップのフリーズが音声
      スコアリングを汚す」ため見送り、定型スキップのみ採用
- [ ] **テンプレートタイトルの多言語対応（en/ru localizations の復活）**
      （2026-07-17 追加）: LLM 廃止で止まった YouTube localizations を
      テンプレートで再実装する。書式は各言語版テンプレート
      （例: en `T95, 2 kills, 2,607 damage, Steel Wall` /
      ru `T95, 2 фрага, 2607 урона, Стальная стена`）。
      勲章名の翻訳は (a) 対象29種（エピック+バトルヒーロー）の en/ru 名を
      静的辞書としてコミット（一度だけ生成しレビュー）、または
      (b) WG 公式 API（encyclopedia/achievements, language パラメータ）から取得。
      まず (a) で十分。upload 側は既存の localizations 機構をそのまま使える

- [x] **タイトルの簡素化 + レア勲章の反映**（2026-07-14 実装）: 車両名を必須化、
      命中率など数字の羅列を廃止、コロバノフ勲章等のレア勲章獲得時は勲章名を入れる。
      勲章 ID → 日本語名の対応表は `tools/extract_achievements.py` がクライアント
      （records.pyc の RECORD_DB_IDS + achievements.mo）から生成 →
      `src/data/achievements.json` + `src/achievements.py`。クライアント更新で
      新勲章が出たらツール再実行
- [x] **タイトルを固定テンプレート化（LLM 不使用）**（2026-07-17 方針確定・実装）:
      「タンク名, キル数, ダメージ数, 勲章（あれば）」形式
      （例: `T95, 2キル, 2,607ダメージ, 鋼鉄の壁`）。config の `youtube.llm_title` を
      false に。LLM 経路（src/llm_title.py）と多言語 localizations はコード残置・停止中。
      不要と確定したら削除する。人気タイトルの API 収集案（few-shot 用）も
      LLM 不使用につき見送り

- [x] **予約投稿（`status.publishAt`）で公開を分散**（2026-07-17 実装・実機検証）:
      `src/schedule.py`（スロット割り当て + `output/schedule_state.json` の最後尾管理、
      過去時刻回避のリード15分、state 消失時は now から自己修復）、
      `build_video_metadata()`/`upload_video()` に publish_at、呼び出し側は
      upload_backlog と pipeline の両方。config は
      `youtube.schedule: { enabled, times: ["HH:MM", ...] }`（JST、本数/日 = リスト長）。
      **運用ノート**: バックログ消化中は times をクォータと同数（6枠）にして
      予約テールが伸びないようにする。消化後は 3 枠程度へ。
      本数を増やしたい場合は別途**クォータ増枠申請**（YouTube API 監査フォーム、
      無料、審査数週間）が唯一の正攻法。
- [ ] **タスクスケジューラでの夜間バッチ自動実行**: Windows タスクスケジューラ
      から `python -u run_batch.py` を定期起動（サブPC運用）。Claude Code の
      常駐セッションは使わない（使用量・信頼性の観点から `claude -p` 方式に限定）
- [x] OBS を window_capture（WoT ウィンドウ指定・WGC）に切り替え（2026-07-04。
      録画中のフォーカス強奪テストで無影響を実証。旧 monitor_capture ソースは
      無効化して温存。config: obs.capture）
- [ ] 音声ピーク検出の閾値チューニング: フルバトル録画で 19 ピーク/戦闘は妥当だが、
      Shorts のようなダイナミックレンジが狭い音声では検出漏れする
      （ノイズフロアを中央値でなく低パーセンタイルにする案）

## mod ベースのハイライト検出（実証済み・統合待ち）

2026-07-04 に mod によるゲーム内射撃イベント取得を実証し、パイプライン統合済み。
E2E 検証: fishing_bay で mod イベント7件 → 誤検出ゼロの7クリップ Shorts を生成。

- [x] pipeline 統合（サイドカー `.meta.json`/`.events.json`、`src/detect_mod_events.py`、
      検出優先順位 mod > 音声+輝度融合 > 輝度のみ）
- [x] preflight に mod 未配置の警告を追加（クライアント更新後の検知）
- [x] **被弾 (hit_taken) 検知**（2026-07-23 実装・実機検証済み）: mod v4 が
      `Vehicle.onHealthChanged` で被弾量・damage_pct を検知（直前HPは
      self.health を信用せず `_last_known_health` で自前追跡。self.health は
      フック時点で newHealth 相当に更新済みと判明したため）。
      (a) 射撃後 `DUEL_WINDOW_SEC`（3秒）以内の被弾で射撃イベントに
      「撃ち合い」加点、(b) 残HPの `HIT_TAKEN_MIN_PCT`（30%）以上を
      一撃で失った被弾は単独ハイライト候補化。実機で T95 の被弾3件
      （damage_pct 24-27%）を検知、撃ち合いボーナス成立も確認。
      閾値30%は今回の実測では未到達（要チューニング候補、データが
      たまってから調整）
- [ ] mod イベントの拡張（命中・貫通の検知）→ ハイライトのスコアリング精度向上。
      被弾検知は実装済み（上記）
- [x] ~~フック優先編成~~（クリップを時系列順でなくスコア降順に並べ替える案）は
      2026-07-25 見送り。時系列順の方が動画として分かりやすいという判断。
      ただしスコアによる優先順位付けの発想自体は、動画の尺を調整する際に
      優先度の低いクリップから間引く「重み付けカット」に転用できそうなので
      下記に転記（イベント密度ベースの可変長クリップの項）
- [ ] バトル開始フック（onArenaPeriodChange）が発火しなかった件の調査（任意。
      epoch 方式では不要だが、リザルト画面除外などに使える）

## 調査・将来

- [ ] **リプレイバイナリ復号の再調査**: 既知の旧 AES キーは v2.3.0.0 で不可。
      コミュニティのパーサ実装・別キー/別暗号（Blowfish 等）の可能性を調査
- [x] 音声ベースのハイライト検出（`src/detect_audio_events.py` として実装済み）
- [x] イベント検知 mod の再挑戦（`mods/` で実現、pipeline 統合済み。上記セクション参照）
- [x] `detect_ui_events.py` は削除で決着（mod イベントで代替）

## 将来構想: 再生数・収益化の方向性（2026-07-13 議論、実装は未着手）

現状のボトルネックは「何が刺さるか分かるデータがない」ことと「視聴維持率」。
Shorts 単体は収益源ではなく集客装置（収益化ライン: 登録1,000人 + 90日で
Shorts 1,000万再生。RPM も低い）。優先順位案:

1. [ ] **Analytics 収集の自動化**: YouTube Analytics API で視聴回数・平均視聴時間・
       スワイプ離脱率を週次で自動収集し、タイトル型/クリップ長/車種との相関を見る。
       全施策の判断土台。ML は数百本たまるまで時期尚早（「データ駆動のパラメータ
       調整」から始める）
2. [x] ~~フック優先編成~~: 2026-07-25 見送り（時系列順の分かりやすさを優先）。
       詳細は上の「mod ベースのハイライト検出」セクション参照
3. [x] **射撃間隔ベースの可変長クリップ**（2026-07-31 実装）: 固定 3s+4s 窓をやめ、
       そのリプレイで実際に観測した「直前の自分の射撃からの経過秒（reload interval）」
       から線形補間でクリップの pre_sec/post_sec を決める（速射・自動装填は短く、
       単発火力の大きい車両は長く）。外部の車両DPMテーブルは持たず、実測間隔ベース
       にすることで新車両追加時のメンテ不要・装填ブースト等の実際の挙動にも追従する。
       基準値に対し ±20%（`JITTER_RATIO`）の比率ジッターを常時付与（interval不明な
       最初の1発にも適用、絶対秒数の固定ジッターだと短尺/長尺で振れ幅の効き方が
       不均一になるため比率にした）。対象は mod イベント (`shot_mod`) のみ、CVフォール
       バック (`shot_flash`) と `hit_taken` 単独ハイライトは従来通り固定長。
       `select_clips()` の尺予算チェックも「スコア上位N本」の固定長前提スライスから
       「スコア降順に舐めて実尺で収まる分だけ採用」の貪欲法に一般化（下記の
       重み付けカット案はこの実装で解消）。詳細: `src/detect_mod_events.py`
       (`pacing_for_interval`, `FAST_RELOAD_SEC`/`SLOW_RELOAD_SEC`/`PRE_SEC_RANGE`/
       `POST_SEC_RANGE`/`JITTER_RATIO`)、`src/edit_video.py`
       (`_effective_pre`/`_effective_post`/`_effective_duration`)。config:
       `shorts.dynamic_clip_length.enabled`（false で固定長に即戻せる）。
       **未実施**: 実機での目視確認（次回バッチの生成物で確認）
4. [ ] **TikTok への半手動転載で市場テスト**: 同じ縦動画を週の上位数本だけ手で転載し
       反応を見る。伸びたら自動化投資（公式投稿 API は開発者審査が必要、
       非公式自動化は BAN リスク）。次点で Instagram Reels
5. [ ] **リアクションキャラ挿入の A/B 検証**: viggle AI 等で喜怒哀楽の固定リアクション
       クリップを事前生成し ffmpeg overlay で合成。ブランド化（登録動機）としては
       筋があるが、AI っぽさへの風当たりと飽和が早いリスクあり。
       Analytics 基盤ができた後に 10本入り/10本なしで維持率を比較してから本採用判断
6. [ ] **X 投稿連携**: 実装は最安（API 無料枠で可）だが、フォロワー0 への通知は
       効果薄。WoT コミュニティのハッシュタグ露出は多少あり。観客ができてから
7. [x] **ロング動画のアップロード**（2026-07-25 着想 → 2026-07-27 実装 →
       **2026-08-04 一時停止**）: 編集なしのフル録画をロング動画としてアップロードし、
       対応する Shorts の説明欄に「フル試合はこちら」リンクを追記する
       `upload_longform_backlog.py` を実装、2026-07-31 に実機確認済み（3本で
       アップロード→Shorts説明欄バックフィル→フル録画削除まで成功）。
       **一時停止の経緯（2026-08-04）**: バックログ消化がスクリプトの手動実行に
       依存するため録画ペースに追いつかず滞留（46→60本）、ディスクを圧迫。
       「Shortsアップロードとインラインでセット化してバックログをなくす」案も
       検討したが、videos.insert が1リプレイあたり2回になりクォータ消費が倍増する
       懸念から一旦保留（後日クォータの実態を再調査したところ、直近5日間
       9〜19本/日をクォータエラーなく安定アップロードできており「クォータは
       実質6本/日」という前提自体が誤りだったと判明。とはいえチャンネル規模が
       まだ小さく（登録者7人程度）、todo.md優先順位表の「面を広げる」は最後回しの
       施策であるため、**当面はロング動画アップロード自体を`youtube.longform.enabled: false`
       で停止し、Shorts戦略に集中する**という結論になった）。
       未処理だった生録画バックログ（60本）は削除済み。関連して検討していた
       「Shorts→ロング動画の関連動画リンクのブラウザ自動化」（Claude in Chromeでの
       手動操作は実機検証済み: `studio.youtube.com/video/<ShortsのvideoID>/edit`→
       右パネル「関連動画」→候補選択→保存、`longform_uploaded.json`にペア情報あり）
       も同時に保留。コード（`src/longform.py`, `upload_longform_backlog.py`）は
       残してあるので、チャンネルが育って「面を広げる」段階になったら
       `enabled: true`に戻すだけで再開できる。
       **2026-08-05実装**: `youtube.longform.enabled: false`の間は生録画を
       自動削除するよう変更（`src/pipeline.py`の`_delete_raw_if_longform_disabled`。
       ハイライト無しなら即削除、Shorts生成時はアップロード成功
       （video_id記録）を確認してから削除）。再開時に`enabled: true`へ戻せば、
       この自動削除は働かなくなり生録画は保持される（ロング動画側が
       アップロード後に削除する既存の仕組みに委ねられる）
8. [ ] **Webから好成績のリプレイファイルを自動取得して動画化**（2026-08-04着想）:
       自分のプレイ以外に、Web上で公開されている好成績のリプレイファイルを自動収集し、
       既存パイプラインにそのまま投入する。現行パイプラインは「`.wotreplay`ファイルを
       受け取れば出どころを問わない」設計（`replays.dir`に置くだけで通常処理される）
       なので、収集スクリプトを1つ追加するだけで済むはず。**ライブ録画方式への移行は
       この観点から見送り**（2026-08-03議論）: ライブ録画は「今まさに自分がプレイして
       いる」前提が必須で、他人が対戦したリプレイには原理的に使えない。Web由来リプレイ
       という将来構想がある限り、リプレイ再生録画の仕組みは結局残す必要があり、
       二重のメンテになるライブ録画への投資は見送り、現行方式のまま信頼性を上げる
       方向（無人WoT再起動の不安定さ等）に注力する方が筋が良いと判断

要約: 「測る → 冒頭を強くする → 切り方を賢くする → 面を広げる」の順。

## 完了済み

- [x] OBS 自動起動 + 録画前プリフライトチェック（`src/preflight.py`。音声設定検査 +
      テスト録音検証。無音録画ガード・前面化ガードをバッチに統合）
- [x] ディスク空き容量プリフライトチェック（2026-07-27）: output/ のフル録画肥大化
      でディスクが満杯になり、53本目以降47本連続失敗する事故が発生。空き容量
      50GB未満ならバッチ開始前に中断するチェックを追加し、リムックス失敗時の
      `.tmp.mp4` 後始末漏れも修正
- [x] `run_batch.py` の glob 化（日付 + クライアントバージョンフィルタ）
- [x] 音声スパイク検出（`src/detect_audio_events.py`。輝度フラッシュと融合。
      実録画で 19 音声ピーク + 12 フラッシュ → 28 イベントを確認）
- [x] `claude -p` LLM タイトル生成（`src/llm_title.py`。実呼び出しで動作確認済み、
      テンプレートフォールバック付き）
- [x] WoT ウィンドウ前面化（フォアグラウンドロック対策。2026-07-03 未明の
      バッチが74本全てターミナル画面を録画した事故の修正）
- [x] YouTube アップロードパイプライン（OAuth2 / 再開可能アップロード / 重複防止）
- [x] OBS 録画無音問題の解決（原因: Windows ミキサーで WoT が個別ミュート。
      OBS は正常。診断: `ffprobe -select_streams a` で bit_rate ~2300 なら無音）
- [x] `feature/youtube-upload` を main にマージ・push
- [x] リファクタリング: `src/config.py` 新設（config 読み込み・ffmpeg 探索の集約）、
      pipeline の責務分割、タイトル生成の二重実行解消
- [x] Shorts 合計時間上限の実装（`select_clips()`、150秒 = 2分半）
- [x] テストスイート修復: 81 passed / 9 skipped / 警告なし
      （腐敗テスト修正・リプレイフィクスチャのコミット・edit_video テスト追加）

---

## テスト方針（メモ）

外部プロセス（WoT・OBS・YouTube API 通信）のラッパーはテスト対象外。
モックにすると「モックのテスト」になり費用対効果が低い。
テストを書くのは外部依存のない純粋ロジックのみ
（parse_replay のパース、edit_video のクリップ選択、upload_youtube のメタデータ構築等）。
